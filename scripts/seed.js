/**
 * This script is no longer needed as we're using mock data
 * Instead, it now just displays a message about the mock data
 */

console.log('Using mock data instead of MongoDB');
console.log('The mock data is located at: src/data/mockModels.js');
console.log('This includes 10 real AI models and 20 generic test models');
console.log('To modify the mock data, edit the mockModels.js file directly');

process.exit(0); 